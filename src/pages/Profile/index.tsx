import { Text, View } from 'react-native';
import { Page } from '../../components/layout';

export default function Profile() {
    return (
        <Page>
            <Text>Profile</Text>
        </Page>
    );
};