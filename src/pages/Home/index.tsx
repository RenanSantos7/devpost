import { Text, View } from 'react-native';
import { Page } from '../../components/layout';

export default function Home() {
    return (
        <Page>
            <Text>Home</Text>
        </Page>
    );
};