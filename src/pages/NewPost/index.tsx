import { Text, View } from 'react-native';
import { Page } from '../../components/layout';

export default function NewPost() {
    return (
        <Page>
            <Text>NewPost</Text>
        </Page>
    );
};